import React, { FC } from "react";
import Profile from "./ProfileSections/Profile";

const ProfilePage: FC = () => {
  return (
    <div>
      <Profile />
    </div>
  );
};

export default ProfilePage;
